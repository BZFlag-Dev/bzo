services:
  bzo:
    image: ghcr.io/timriker/bzo:latest
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      PORT: "3000"
      SERVER_CONFIG_PATH: /data/server.json
    volumes:
      - ./data:/data
